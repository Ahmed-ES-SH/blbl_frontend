import { slidercategorycontent } from "@/app/constant/websitecontent";
import { Search, X } from "lucide-react";
import React from "react";

export default function SearchBox({ setstate }: any) {
  return (
    // <div className="w-1/2 max-md:w-full my-3 px-4  m-auto   relative">
    //   <div className="w-full bg-bglight border px-4 py-2 z-[9999] shadow-md rounded-md">
    //     <div
    //       style={{ direction: "ltr" }}
    //       className="w-full h-[40vh] px-3 py-3 mt-6 "
    //     >
    //       <select className="w-full p-3 my-3 rounded-md outline-none bg-[#ddd] ">
    //         <option disabled>إختر أحد الأقسام </option>
    //         {slidercategorycontent.map((item, index) => (
    //           <option key={index}>{item.title}</option>
    //         ))}
    //       </select>
    //       <select className="w-full p-3 my-3 rounded-md outline-none bg-[#ddd] ">
    //         <option disabled>إختر أحد الخدمات </option>
    //         {randomservices.map((item, index) => (
    //           <option key={index}>{item.text}</option>
    //         ))}
    //       </select>
    //       <div className="px-3 py-3 mt-6 border-t border-black">
    //         <input
    //           className="px-4 py-2 cursor-pointer w-fit  bg-main_color text-white rounded-md"
    //           type="submit"
    //           value={"التالى"}
    //         />
    //       </div>
    //     </div>
    //   </div>
    //   <div className="flex-two  absolute top-6 left-8">
    //     <X
    //       onClick={() => setstate("")}
    //       className="  text-gray-400 cursor-pointer"
    //     />
    //   </div>
    // </div>
    <div></div>
  );
}
